<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
<div class="container">
  <a href="{{ path("homepage") }}" class="navbar-brand">TaskList</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav ml-auto">
			{% set route = app.request.get('_route') %}
			
			<li class="nav-item{% if route == 'homepage' %} active{% endif %}">
        		<a class="nav-link" href="{{ path("homepage") }}">{{ 'Home' }} </a>
      		</li>
      		
			{# {% if is_granted('IS_AUTHENTICATED_FULLY') %} #}
			<li class="nav-item{% if route == 'display_tasks' %} active{% endif %}">
        		<a class="nav-link" href="{{ path("display_tasks") }}">{{ 'My Tasks' }} </a>
      		</li>
			{# {% endif %}
      		
      		{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
      		<li class="nav-item{% if route == 'register' %} active{% endif %}">
        		<a class="nav-link" href="{{ path("homepage") }}">{{ 'Register' }}</a>
      		</li>
      		
      		<li class="nav-item{% if route == 'login' %} active{% endif %}">
        		<a class="nav-link" href="{{ path("homepage") }}">{{ 'Login' }}</a>
      		</li>
      		{% endif %}      		
      		      		
      		{% if is_granted('IS_AUTHENTICATED_FULLY') %}
      		<li class="nav-item">
        		<a class="nav-link" href="{{ path("homepage") }}">{{ 'Logout' }}</a>
      		</li>
      		{% endif %} #}  		
    	</ul>
  	</div>
</div>  	
</nav>
